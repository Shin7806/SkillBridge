
  # Build accessible reusable components

  This is a code bundle for Build accessible reusable components. The original project is available at https://www.figma.com/design/TCqJ8NI0LZedHGvTuWe8Yl/Build-accessible-reusable-components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  